# Lightweight serving dependencies for the NSRM Risk-Sensing backend.
# Only what routers/services actually import.
# (Pipeline packages such as selenium/konlpy/langgraph/lightrag are NOT needed for serving.)
fastapi>=0.138.0
uvicorn[standard]>=0.49.0
pydantic>=2.13.0
openai>=2.0.0
python-dotenv>=1.2.0
